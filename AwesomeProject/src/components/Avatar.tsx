import * as React from 'react';
import {Avatar} from 'react-native-paper';

const AvatarImage = () => (
  <Avatar.Image size={200} source={require('../assets/image/chopper.jpg')} />
);
export default AvatarImage;
